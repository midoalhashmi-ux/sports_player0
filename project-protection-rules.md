# قواعد حماية بنية المشروع — لأي نموذج ذكاء اصطناعي يعمل عليه

هذا الملف مرجع ثابت لمشروعك (تطبيق المحتوى `CLOUDAPP2026` + تطبيق المشغل `sports_player` + لوحة التحكم). الصقه في بداية أي محادثة مع أي نموذج ذكاء اصطناعي (مجاني أو غيره) قبل ما يبدأ أي تعديل. الهدف: يقدر يطوّر ميزات جديدة بحرية، لكن ما يلمس أي شيء يكسر بناء Codemagic أو الربط بين التطبيقات الثلاثة.

---

## 1) ملفات ومناطق محمية — ممنوع تعديلها إلا بإذن صريح مسبق منك

| الملف/المنطقة | ليه محمي |
|---|---|
| `lib/main.dart` (في كلا التطبيقين) | نقطة بدء التطبيق والتنقل الأساسي — أي تغيير هنا يؤثر على كل الشاشات |
| `lib/firebase_options.dart` | مولّد تلقائياً من Firebase، يجب يطابق مشروعك بالضبط (`sports-stream-app-36a7a`) |
| `android/app/google-services.json` | إعداد Firebase لأندرويد — أي تعديل يدوي يكسر الاتصال بـ Firebase |
| `android/build.gradle` | إعدادات Gradle الجذرية (المستودعات، إصدارات classpath) |
| `android/app/build.gradle` | **الأهم**: `applicationId`, `minSdkVersion`, `targetSdkVersion`, `compileSdkVersion`, إعدادات التوقيع (`signingConfigs`) |
| `android/gradle/wrapper/gradle-wrapper.properties` (إن وُجد) | إصدار Gradle نفسه |
| `codemagic.yaml` | خطوات البناء، إصدار Flutter المستخدم، مسار الأرتيفاكتس |
| ملفات التوقيع (`key.properties`, أي `.jks` أو `.keystore`) | بيانات اعتماد حساسة، وأي خطأ فيها يمنع البناء نهائياً |
| أرقام إصدارات الحزم **الموجودة أصلاً** في `pubspec.yaml` | ترقية حزمة شغّالة قد تكسر حزم أخرى تعتمد عليها (تعارض إصدارات) |
| أسماء الحقول والمجموعات في Firestore (`channels`, `settings/ads`, `settings/app`, `settings/legal`, `contact_messages`, إلخ) | ثلاث مشاريع منفصلة (تطبيق المحتوى، تطبيق المشغل، لوحة التحكم) تتفق على نفس الأسماء بالضبط — تغييرها في مشروع واحد يكسر الاثنين الآخرين |

---

## 2) قواعد عامة لأي نموذج يعمل على المشروع

1. **ممنوع ترقية إصدار أي حزمة موجودة مسبقاً** في `pubspec.yaml` بدون طلبك الصريح — حتى لو ادّعى النموذج أنها "قديمة" أو "فيها ثغرة" أو "أفضل". يقترح فقط، ولا يعدّل.
2. **ممنوع إضافة أو حذف أي حزمة** بدون ما يذكر لك اسمها وسبب إضافتها وينتظر موافقتك.
3. **ممنوع تغيير** `compileSdkVersion` / `minSdkVersion` / `targetSdkVersion` / إصدار Kotlin أو Java في أي من ملفي `build.gradle`.
4. **ممنوع تغيير** `applicationId` أو اسم الحزمة (package name) في أي مكان.
5. **ممنوع لمس** ملفات التوقيع أو أي بيانات اعتماد.
6. **ممنوع تعديل** `codemagic.yaml` (خطوات البناء، إصدار Flutter، الأرتيفاكتس) بدون إذن صريح.
7. **ممنوع إعادة تسمية أو حذف** أي حقل/مجموعة في Firestore يُستخدم من تطبيق آخر — فقط إضافة حقول جديدة مسموحة بحرية.
8. أي تعديل يحتاج يمس بندًا من القسم (1): **يتوقف النموذج ويشرح لك بالضبط** أي ملف/سطر ولماذا، وينتظر "نعم" صريحة منك قبل ما يسلّمك أي شيء متعلق فيه.
9. كل تسليم يكون **فقط الملفات الجديدة أو المعدّلة** (لا إعادة كتابة المشروع كامل)، مع ذكر المسار الكامل لكل ملف بالضبط.
10. **ممنوع اقتراح ترقية Flutter SDK نفسه** أو أدوات البناء بدون طلب صريح منك.
11. إذا واجه النموذج مشكلة بناء وسبب حلها الوحيد يمس أحد الملفات المحمية — يخبرك بالمشكلة والحل المقترح ويطلب إذنك، ولا يفترض الموافقة.

---

## 3) نص جاهز تلصقه في بداية أي محادثة جديدة مع أي نموذج

> اعمل على مشروعي حسب الملف المرفق "قواعد حماية بنية المشروع". لا تعدّل أي ملف من قائمة الملفات المحمية، ولا ترقّي أي حزمة موجودة، ولا تغيّر إعدادات البناء أو التوقيع أو معرّف التطبيق، إلا إذا طلبت ذلك صراحة أنا بنفسي. أي تعديل يحتاج يمس هذي المناطق، أوقف واشرح لي السبب قبل ما تسلّمني أي ملف. سلّمني فقط الملفات الجديدة/المعدّلة مع ذكر مسارها الكامل.



### Auto Source Engine V3 safety invariant
Only public media URLs exposed by the page are considered. DRM detection forces WebView-only mode; no DRM keys or encrypted media are extracted or bypassed.
